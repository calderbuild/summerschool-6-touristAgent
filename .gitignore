.DS_Store
node_modules/
.env
*.log
__pycache__/

# Internal research/reference docs — kept locally, never pushed.
# Team repos fork from a shared upstream, so GitHub's fork network makes
# every team's repo one click away from every other. Blanket rule (not a
# per-file list) so any new file dropped into docs/ is protected by default.
docs/*.md
